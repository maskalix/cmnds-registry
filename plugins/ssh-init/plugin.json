{
  "name": "ssh-init",
  "version": "2.0.0",
  "description": "Generate an SSH keypair for a user and register it in authorized_keys",
  "author": "Martin Skalicky",
  "type": "binary",
  "entry_point": "ssh-init",
  "category": "ssh",
  "dependencies": {
    "apt": ["openssh-server"],
    "plugins": []
  },
  "help": {
    "usage": "ssh-init",
    "description": "Generate SSH key pair, configure SSH server settings, and set up SSH authentication. Creates RSA keys and adds public key to authorized_keys.",
    "options": [
      {
        "flag": "-h, --help",
        "description": "Show this help message"
      }
    ],
    "examples": [
      {
        "command": "ssh-init",
        "description": "Set up SSH keys and server configuration"
      }
    ]
  }
}
