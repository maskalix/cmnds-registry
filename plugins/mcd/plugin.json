{
  "name": "mcd",
  "version": "1.0.0",
  "description": "Create a directory and change into it",
  "author": "Martin Skalicky",
  "type": "script",
  "entry_point": "mcd.sh",
  "category": "files",
  "dependencies": {
    "apt": [],
    "plugins": []
  },
  "help": {
    "usage": "mcd <directory>",
    "description": "Creates a new directory (including parent directories if needed) and immediately changes into it. If the directory already exists, it just changes into it.",
    "options": [
      {
        "flag": "-h, --help",
        "description": "Show this help message"
      }
    ],
    "examples": [
      {
        "command": "mcd my-project",
        "description": "Create 'my-project' directory and cd into it"
      },
      {
        "command": "mcd /tmp/test/nested",
        "description": "Create nested directories and cd into the deepest one"
      }
    ]
  }
}
