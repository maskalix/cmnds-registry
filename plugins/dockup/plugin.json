{
  "name": "dockup",
  "version": "1.0.0",
  "description": "Docker compose up (interactive)",
  "author": "Martin Skalicky",
  "type": "script",
  "entry_point": "dockup.sh",
  "category": "docker",
  "dependencies": {
    "apt": ["docker.io", "docker-compose"],
    "plugins": []
  },
  "help": {
    "usage": "dockup",
    "description": "Runs 'docker compose up' in interactive mode (shows logs in foreground).",
    "options": [
      {
        "flag": "-h, --help",
        "description": "Show this help message"
      }
    ],
    "examples": [
      {
        "command": "dockup",
        "description": "Start Docker compose services interactively"
      }
    ]
  }
}
