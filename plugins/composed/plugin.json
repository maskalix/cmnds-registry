{
  "name": "composed",
  "version": "1.0.0",
  "description": "Docker compose up in detached mode",
  "author": "Martin Skalicky",
  "type": "script",
  "entry_point": "composed.sh",
  "category": "docker",
  "dependencies": {
    "apt": ["docker.io", "docker-compose"],
    "plugins": []
  },
  "help": {
    "usage": "composed",
    "description": "Runs 'docker compose up -d' to start Docker containers in detached mode.",
    "options": [
      {
        "flag": "-h, --help",
        "description": "Show this help message"
      }
    ],
    "examples": [
      {
        "command": "composed",
        "description": "Start Docker compose services in background"
      }
    ]
  }
}
