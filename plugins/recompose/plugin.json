{
  "name": "recompose",
  "version": "1.0.0",
  "description": "Docker compose restart (down + up)",
  "author": "Martin Skalicky",
  "type": "script",
  "entry_point": "recompose.sh",
  "category": "docker",
  "dependencies": {
    "apt": ["docker.io", "docker-compose"],
    "plugins": []
  },
  "help": {
    "usage": "recompose",
    "description": "Runs 'docker compose down' followed by 'docker compose up -d' to restart all services.",
    "options": [
      {
        "flag": "-h, --help",
        "description": "Show this help message"
      }
    ],
    "examples": [
      {
        "command": "recompose",
        "description": "Restart all Docker compose services"
      }
    ]
  }
}
