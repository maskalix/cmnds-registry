{
  "name": "remount",
  "version": "2.0.0",
  "description": "Unmount a path and re-run mount -a from /etc/fstab",
  "author": "Martin Skalicky",
  "type": "binary",
  "entry_point": "remount",
  "category": "files",
  "dependencies": {
    "apt": [],
    "plugins": []
  },
  "help": {
    "usage": "remount <mountpoint>",
    "description": "Unmounts the given path then reapplies /etc/fstab via mount -a. Useful after fstab edits or when a single mount becomes stale.",
    "options": [
      { "flag": "-h, --help", "description": "Show this help" }
    ],
    "examples": [
      { "command": "remount /mnt/data", "description": "Remount /mnt/data" }
    ]
  }
}
